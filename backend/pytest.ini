[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external deps)
    integration: Integration tests (require running services)
    slow: Slow tests that load real ML models (real embedder / E2E)
